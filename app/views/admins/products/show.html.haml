%h2= @product.name
%h2= image_tag @product.image.url if @product.image.present?
%p= currency(@product.price)
%p 在庫:#{@product.stock}
= link_to '編集', edit_admins_product_path
= link_to '削除', admins_product_path, method: :delete, data: { confirm: '削除してもよろしいでしょうか？' }
